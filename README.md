# CyberShield SIH26106 Demo

## Run
pip install -r requirements.txt
uvicorn backend:app --reload
Open http://127.0.0.1:8000

Use **Run Demo Email** for the prepared phishing scenario. Upload an `.eml` file to test parsing.

This is an MVP: threat-intelligence geolocation and blockchain are clearly labelled demo/simulated. Replace those parts with live APIs/testnet before claiming production capability.
